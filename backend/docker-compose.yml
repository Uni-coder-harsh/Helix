version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - HELIX_ENV=dev
      - LOG_LEVEL=DEBUG
      - DATABASE_URL=sqlite:///helix.db
    volumes:
      - .:/app
    restart: always
